import { styled } from '../../styles';

export const Container = styled('section', {
  display: 'flex',
  height: '100vh',
  maxWidth: '1240px',
  margin: '0 auto',
  padding: '0 16px',
});

export const Board = styled('section', {
  display: 'flex',
  height: '100vh',
  maxWidth: '1240px',
  margin: '0 auto',
  padding: '0 16px',
});